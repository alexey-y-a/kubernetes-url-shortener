apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Values.postgres.name }}-pvc"
  namespace: my-namespace
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
